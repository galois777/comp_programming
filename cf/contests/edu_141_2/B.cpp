#include<iostream>
#include<algorithm>
#include<vector>
#include<cstdio>

using namespace std;

void solve(){
    int n;
    cin >> n;
    vector a(n,vector<int>(n));
    int l =1, r = n*n;
    int t = 0;
    for (int i = 0; i < n; ++i){





int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) solve();

    return 0;
}
